<%= form_with(model: seniority, local: true) do |form| %>
  <% if seniority.errors.any? %>
    <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="govuk-error-summary">
      <h2 class="govuk-error-summary__title" id="error-summary-title">
        <%= pluralize(seniority.errors.count, "error") %> prohibited this person from being saved:
      </h2>
      <div class="govuk-error-summary__body">
        <ul class="govuk-list govuk-error-summary__list">
          <% seniority.errors.full_messages.each do |message| %>
            <li>
              <%= message %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="govuk-form-group">
    <%= form.label :name, class: "govuk-label" %>
    <%= form.text_field :name, class: "govuk-input govuk-!-width-one-half" %>
  </div>

  <div class="govuk-form-group">
    <%= form.label :prefix, class: "govuk-label" %>
    <%= form.text_field :prefix, class: "govuk-input govuk-!-width-one-half" %>
  </div>

  <div class="govuk-form-group">
    <%= form.label :sort_index, class: "govuk-label" %>
    <%= form.text_field :sort_index, class: "govuk-input govuk-!-width-one-half" %>
  </div>

  <div class="govuk-form-group">
    <%= form.submit class: "govuk-button" %>
  </div>
<% end %>
