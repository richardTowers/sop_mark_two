<%= form_with(model: team, local: true) do |form| %>
  <% if team.errors.any? %>
    <div class="govuk-error-summary" aria-labelledby="error-summary-title" role="alert" tabindex="-1" data-module="govuk-error-summary">
      <h2 class="govuk-error-summary__title" id="error-summary-title">
        <%= pluralize(team.errors.count, "error") %> prohibited this person from being saved:
      </h2>
      <div class="govuk-error-summary__body">
        <ul class="govuk-list govuk-error-summary__list">
          <% team.errors.full_messages.each do |message| %>
            <li>
              <%= message %>
            </li>
          <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="govuk-form-group">
    <%= form.label :name, class: "govuk-label" %>
    <%= form.text_field :name, class: "govuk-input govuk-!-width-one-half" %>
  </div>

  <div class="govuk-form-group">
    <%= form.label :start_time, class: "govuk-label" %>
    <%= form.date_select :start_time %>
  </div>

  <div class="govuk-form-group">
    <%= form.label :end_time, class: "govuk-label" %>
    <%= form.date_select :end_time %>
  </div>

  <div class="govuk-form-group">
    <%= form.label :description, class: "govuk-label" %>
    <%= form.text_area :description, class: "govuk-textarea" %>
  </div>

  <div class="govuk-form-group">
    <%= form.submit class: "govuk-button" %>
  </div>
<% end %>
